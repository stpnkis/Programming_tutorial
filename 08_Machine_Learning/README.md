# 08 - Machine Learning

Od dat k modelům — klasický ML i deep learning.

## Témata
| # | Složka | Co se naučíš |
|---|--------|-------------|
| 01 | `pandas_data_prep` | DataFrame, čištění dat, merge, groupby |
| 02 | `sklearn_zaklady` | fit/predict/transform pipeline, train_test_split |
| 03 | `regrese` | LinearRegression, Ridge, Lasso, metriky |
| 04 | `klasifikace` | LogReg, SVM, Random Forest, confusion matrix |
| 05 | `clustering` | KMeans, DBSCAN, hierarchické, silhouette |
| 06 | `model_evaluation` | cross-validation, overfitting, hyperparametrry |
| 07 | `feature_engineering` | scaling, encoding, PCA, feature selection |
| 08 | `neuronove_site_pytorch` | tensory, nn.Module, loss, optimizer, training loop |
| 09 | `CNN` | Conv2d, pooling, architektury (ResNet, VGG) |
| 10 | `transfer_learning` | pretrained modely, fine-tuning, torchvision |

## Cíl
Umět celý ML pipeline: data → preprocessing → model → evaluace → deploy.
